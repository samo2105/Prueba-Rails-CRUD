<div class="container">

  <%= link_to 'New To_do', todos_new_path %>
  <br>
  <% @todos.each do |todo| %>
  <div class="card bg-primary text-white text-center">
  <p><%= todo.description %> /
  <%= todo.completed %></p>
  </div>
  <br>
<% end %>
</div>